"use client";

import { ThemeProvider, Input, Menu, MenuHandler, MenuItem, MenuList, Button } from "@material-tailwind/react";

export { ThemeProvider, Input, Menu, MenuHandler, MenuItem, MenuList, Button };
